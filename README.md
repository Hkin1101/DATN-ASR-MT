1. Setup:
Overall> 
pip install pandas openpyxl tqdm sacremoses

pip install openai-whisper

pip install sacrebleu

2. Run:
python main/run.py  

3. Error:
[WinError 2] The system cannot find the file specified 
-> window powershell -> run as administrator -> winget install "FFmpeg (Essentials Build)"